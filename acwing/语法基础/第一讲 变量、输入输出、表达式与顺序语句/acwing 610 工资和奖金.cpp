#include <iostream>
#include <cstdio>
using namespace std;
int main()
{
    string name 
    double a, b;
    cin>>name >>a >>b;
    double x = a + b * 0.15;
    printf("TOTAL = R$ %.2lf", x);
    return 0;
}